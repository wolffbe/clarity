# Clarity app whitelist: EXAMPLE / template.
#
# Copy this file to whitelist.txt (same folder). whitelist.txt is gitignored,
# so your real, personal app list never gets committed or published.
#
# One package name per line. Text after # is a comment. Blank lines ignored.
#
# The reliable way to fill whitelist.txt is straight from your device, which
# lists exactly the apps you installed (run with the phone connected by USB):
#
#   adb shell pm list packages -3 | sed "s/package://" > app/src/main/assets/whitelist.txt
#
# Then open it and delete any lines you do NOT want allowed.

com.spotify.music             # Spotify
com.google.android.gm         # Gmail
com.google.android.apps.maps  # Google Maps
